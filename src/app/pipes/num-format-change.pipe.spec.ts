import { NumFormatChangePipe } from './num-format-change.pipe';

describe('NumFormatChangePipe', () => {
  it('create an instance', () => {
    const pipe = new NumFormatChangePipe();
    expect(pipe).toBeTruthy();
  });
});
